apiVersion: helx.renci.org/v1
kind: HelxApp
metadata:
  name: nginx
  namespace: jeffw
spec:
  name: nginx
  services:
    - name: web
      image: nginx:latest
      ports:
        - containerPort: 80
          hostPort: 80
