apiVersion: helx.renci.org/v1
kind: HelxApp
metadata:
  name: nginx1
  namespace: jeffw
spec:
  name: nginx
  services:
    - name: web
      image: nginx:latest
      ports:
        - containerPort: 80
          hostPort: 80
      volumes:
        mnt: bubba:/mnt/bubba
